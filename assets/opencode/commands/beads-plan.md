---
description: Decompose a request into dependency-aware beads and create them with br
subtask: true
---

Load the `beads-plan` skill and execute it for this request:

$ARGUMENTS

Repository context to use if present:

!`br stats 2>/dev/null || true`
!`test -f README.md && sed -n '1,80p' README.md`
!`test -f AGENTS.md && sed -n '1,120p' AGENTS.md`
